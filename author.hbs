{{!< default}}

{{#author}}
<section class="masthead container">
    {{#if profile_image}}<img class="masthead-avatar" src="{{profile_image}}" alt="{{name}}" />{{/if}}
    <span class="kicker">Byline</span>
    <h1>{{name}}</h1>
    {{#if bio}}<p class="masthead-dek">{{bio}}</p>{{/if}}
</section>
{{/author}}

<section class="container feed">
    <div class="section-head">
        <h2>Dispatches</h2>
        <span class="kicker kicker--muted">{{plural pagination.total empty='No posts' singular='% post' plural='% posts'}}</span>
    </div>
    <div class="post-grid">
        {{#foreach posts}}
            {{> "post-card"}}
        {{/foreach}}
    </div>
    {{pagination}}
</section>
